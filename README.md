# legacy-profile-card-disaster
You just landed your first junior dev job. Your tech lead hands you a legacy component built by a "prompt engineer" who lied on their resume. It's a `&lt;div>` soup held together with negative margins, and the padding breaks the layout. Clone it, fix the HTML semantics, and rebuild it with Flexbox before the client sees it.
